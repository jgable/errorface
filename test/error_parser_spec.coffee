should = require "should"
ErrorParser = require "../lib/errorParser"

describe "ErrorParser", ->
    parser = null

    before ->
        parser = new ErrorParser()

    it "can parse invalid request error"

    it "can parse error in request handler"
